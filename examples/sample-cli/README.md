# Sample Consumer

This is a simple Python project that references the `curtrail` libary
locally (by local file path). 

It shows an example of how to use the `curtrail` library and is configured
to run against local test data.

## Example run

```bash
uv run s3_last_data_access.py --days "January" --region "ap-southeast-2"
```
