Note that our test CUR data is in JSONL format (rather than the Parquet that is actually
generated by the CUR system). That allows us to manually inspect/add to the data
without having to re-generate it.

We introduce an extra sub-class that enables this loading - but this would not be used
outside of testing.
